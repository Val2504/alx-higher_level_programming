#!/usr/bin/python3
def print_last_digit(number):
    last_digit = number % 10
    if last_name < 0:
        return abs(last_digit)
    else:
        print(last_digit, end='')
        return last_digit
